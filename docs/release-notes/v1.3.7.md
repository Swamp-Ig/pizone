# Release notes — v1.3.7

**Compare range:** https://github.com/Swamp-Ig/pizone/compare/v1.3.6...v1.3.7

## Since v1.3.6

- Add `ControllerAlreadyClaimedError` for duplicate controller claim/discovery attempts (replaces bare `RuntimeError` with message `already created` on controller paths; discovery-service singleton errors unchanged)
